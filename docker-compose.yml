version: "3"

services:
  server:
    build:
      context: .
    ports:
      - "8000:5050"
    volumes:
      - .:/server
    command: >
      sh -c "python doctor_sync.py"
      # sh -c "python test_doctor.py"